/** @type {import('next').NextConfig} */
const nextConfig = {
  // Do NOT use output: 'export' — API routes must stay server-side for Netlify
  reactStrictMode: true,
};

module.exports = nextConfig;
